export enum BlockchainNodeMetricType {
    Total = 'TOTAL',
    Healthy = 'HEALTHY',
    Unhealthy = 'UNHEALTHY',
}

export enum TransactionSendStatusMetricType {
    Success = 'SUCCESS',
    Fail = 'FAIL',
}
