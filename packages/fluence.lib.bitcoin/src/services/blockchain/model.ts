import { Scheme } from '@applicature-private/fluence.lib.services';

export const BITCOIN = 'BITCOIN';

// tslint:disable-next-line:no-empty-interface
export interface BitcoinBlock extends Scheme.BlockchainBlock {
    
}
