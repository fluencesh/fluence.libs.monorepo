import { MetricController } from '../../../src';
import { ResponseMock } from '../../mocks/response.mock';

describe('case', () => {
    it('should work', () => {
        expect(1).toEqual(1);
    });
});
