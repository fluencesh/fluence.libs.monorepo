import { BitcoinTransport } from '@applicature-restricted/multivest.blockchain.bitcoin';

// tslint:disable-next-line:no-empty-interface
export interface LitecoinTransport extends BitcoinTransport {
}
