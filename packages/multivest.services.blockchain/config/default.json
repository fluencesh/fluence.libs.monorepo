{
  "multivest": {
    "mongodb": {
      "url": "mongodb://localhost/fluence",
      "options": null
    },
    "metric": {
      "help": {
        "blockchainCalls": "collects blockchain calls metric",
        "client": "collects client metric",
        "project": "collects project metric",
        "nodeHealth": "collects node health metric",
        "webhook": "collects webhook metric",
        "subscription": "collects subscription metric"
      }
    }
  }
}